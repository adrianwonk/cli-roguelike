ncurses is used

1. To make build folder
  run: ./createBuild

2. To remove build folder
  run: ./removeBuild

3. To build .exe using build folder
  run: ./buildProject

4. To run the program
  run: ./start

5. To run the program with GDB
  cd into the build directory, and run GDB ./main
